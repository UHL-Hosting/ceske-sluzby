- **Testing environment**: Initialized standard PHPUnit testing framework since none existed in the root previously. Used `tests/bootstrap.php` for minimal WP stubs.
- **Edge cases tested**: The JSON error response from `Ceske_Sluzby_Json_Loader::parse()` when given invalid JSON is now verified using `expectException()`.
